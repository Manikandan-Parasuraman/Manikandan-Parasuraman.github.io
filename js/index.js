// Script specifically for index.html functionality
console.log("Welcome to Manikandan P's Portfolio!");

document.addEventListener("DOMContentLoaded", () => {
    // Current behavior relies on meta refresh redirect in index.html.
    // If further logic is needed before redirecting, it can be added here.
});
